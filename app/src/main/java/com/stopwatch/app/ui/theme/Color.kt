package com.stopwatch.app.ui.theme

import androidx.compose.ui.graphics.Color

// Premium Sky Blue Gradient Color System

// Primary Gradient Colors - Lighter Sky Blue
val PrimaryGradientStart = Color(0xFF4FC3F7)  // Sky blue
val PrimaryGradientEnd = Color(0xFF81D4FA)    // Lighter sky blue

// Secondary & Supporting Colors
val SecondaryBlue = Color(0xFFB3E5FC)         // Very light sky blue
val BackgroundLightBlue = Color(0xFFF0F8FF)   // Alice blue background (very light)
val AccentAmber = Color(0xFFFFA726)           // Amber accent

// Text Colors
val TextPrimaryDark = Color(0xFF01579B)       // Medium dark blue for text (better contrast with lighter bg)
val TextPrimaryLight = Color.White
val TextSecondary = Color(0xFF546E7A)         // Blue-gray for secondary text

// Overlay Colors
val OverlayBlur = Color(0xCC29B6F6)           // Blurred overlay (80% opacity sky blue)
val OverlayGradient = Color(0xCC000000)       // 80% black for gradient overlays
val ShadowColor = Color(0x80000000)           // Shadow color (50% black)

// Legacy/Compatibility Colors (for gradual migration)
val Teal = Color(0xFF00897B)
val SoftCoral = Color(0xFFEF6C57)
val Gold = Color(0xFFFFC107)

// Ivory White (existing feature)
val IvoryWhite = Color(0xFFFFFFF0)
